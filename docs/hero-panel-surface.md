# Hero stacked copy cluster

On viewports below 1100px, the benefit headline and body sit in `.hero-copy__cluster` with no card chrome (no border, radius, fill, or extra padding). The cluster is full width of the copy column and is not a container query root. The stack is centered. The AI pill sits on the photo under the title. CTA and stats follow with tight gaps. The navy hero gradient is unchanged.

Desktop: `.hero-copy__cluster` is `display: contents` (no card).
